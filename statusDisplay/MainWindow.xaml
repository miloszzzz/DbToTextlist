<Window x:Class="statusDisplay.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:statusDisplay"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="10">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Label Grid.Column="0" Grid.Row="0">Wklej DB:</Label>
        <TextBox TextChanged="textChangedEventHandler" x:Name="dbText" Grid.Column="0" Grid.Row="1" 
                 AcceptsReturn="True" AcceptsTab="True" VerticalScrollBarVisibility="Auto"/>

        <Grid Grid.Column="1" Grid.Row="1" Width="Auto" Height="Auto" HorizontalAlignment="Center" 
              VerticalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <Button Click="clickCopySclHandler" x:Name="copyScl" Grid.Row="0" Margin="5" Padding="10">Kopiuj SCL</Button>
            <Button Click="clickCpoyTextlistHandler" x:Name="copyTextlist" Grid.Row="1" Margin="5" 
                    Padding="10">Kopiuj listę tekstową</Button>
        </Grid>
        
        <Grid Grid.Column="2" Grid.Row="1" Margin="5" >
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Label Grid.Row="0">SCL:</Label>
            <TextBox x:Name="sclText" Grid.Row="1" Height="160" IsEnabled="False" AcceptsReturn="True" AcceptsTab="True" 
                     IsReadOnly="True" VerticalScrollBarVisibility="Auto"/>
            <Label Grid.Row="2">Lista tekstowa:</Label>
            <TextBox x:Name="textlistText" Grid.Row="3" Height="160" IsEnabled="False" IsReadOnly="True" 
                     AcceptsReturn="True" AcceptsTab="True" VerticalScrollBarVisibility="Auto"/>
        </Grid>
    </Grid>
</Window>
